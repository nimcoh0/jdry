#if ($suite.getNamespace())
package $this.mangle($suite.getNamespace());
#end

public interface $this.mangle($suite.getName()+"ListenerService") {

#foreach ($listener in $suite.getListeners())
#set ($name = $listener.getFullName())
#set ($response = $listener.getResponse().getType())
$response ${this.mangle($name)}(##
#foreach ($p in $listener.getRequest().getArguments())##
#*      *#$p.getType() $p.getName()#if ($foreach.hasNext), #end ##
#end
) throws Exception;
#end
}